<%= form_with(model: peer_evaluation, local: true) do |form| %>
  <% if peer_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peer_evaluation.errors.count, "error") %> prohibited this peer_evaluation from being saved:</h2>

      <ul>
        <% peer_evaluation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
