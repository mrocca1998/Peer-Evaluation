<!DOCTYPE html>
<html>
  <head>
    <title>Peer Evaluation</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <p class="navbar-text float-right">
    <% if student_signed_in? %>
      Logged in as <strong><%= current_student.email %></strong>.
      <%= link_to 'Edit profile', edit_student_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Logout", destroy_student_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Sign up", new_student_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Login", new_student_session_path, :class => 'navbar-link'  %>
    <% end %>
  </p>
    <table>
      <tr class = "header">
        <%#if current_user.isAdmin %>
        <th><%= link_to "Students", "/students" %></th>
        <th><%= link_to "Groups", "/groups" %></th>
        <th><%= link_to "Projects", "/projects" %></th>
        <th><%= link_to "Scores", "/scores" %></th>
        <%#else%>
        <%#end%>
      </tr>
    </table>
    <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
  <% end %>
    <%= yield %>
  </body>
</html>


